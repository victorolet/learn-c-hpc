#include <stdio.h>

int main (void)
{
int x = 5;
int y = 5;

if (x==y)
{
printf("Your inputs are the same value of %d and %d, summing to: %d\n",x,y,x+y);
}
else
printf("Your input values are different, i.e. %d and %d\n", x,y);


return 0;
}
